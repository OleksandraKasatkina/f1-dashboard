version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    # 'volumes' directive maps local project folder to /app in the container
    # hot-reloading